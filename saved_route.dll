#include<stdio.h>
#include<stdlib.h>
int main()
{
    FILE *fp=fopen("route.txt", "r");
    if(fp!=NULL)
    {
        chetene_na_file(fp);
    }
    else
    printf("File not exsist\n");
return 0;
}
